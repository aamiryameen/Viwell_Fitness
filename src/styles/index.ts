// All common styles will be placed here like for common botton styles


import { Dimensions } from "react-native";
export const SCREEN_WIDTH = Dimensions.get('window').width;
export const SCREEN_HEIGHT = Dimensions.get('window').height;

import { VIWELL_COLORS } from './colors';
import { FontWeights, FontSizes,VIWELL_FONTS } from './fonts';

export { VIWELL_COLORS, FontWeights, FontSizes };
